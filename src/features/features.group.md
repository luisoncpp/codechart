---
id: features
label: Features
color: "#166534"
icon: layers
groups:
  - graph_canvas
  - inspection_panel
  - project_loader
descriptionShort: User-facing UI features
---

Container for the user-facing feature modules: graph canvas, inspection panel, and project loader. Composed from its child groups via group references, so it claims no leaf module directly.

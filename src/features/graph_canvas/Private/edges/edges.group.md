---
id: canvas_edges
label: Edge Layer
color: "#059669"
icon: share
descriptionShort: Custom SVG edge rendering & styling
---

The custom SVG edge layer (React Flow receives `edges={[]}`): floating border anchors, bowed soft-edge paths, arrow heads, style buckets with caching, selection/diff-aware edge coloring (`styleEdge`), and live node-box extraction for endpoint math.

---
id: project_search
label: Project Search
color: "#db2777"
icon: layout
descriptionShort: Ctrl+Shift+F find bar & go-to-file
---

The global find bar: debounced full-text search over module sources (Ctrl+Shift+F) and local file-name filtering for Go to file (Ctrl+P), with match navigation that centers each hit via `focusOn`. Public surface is `index.ts` (`ProjectSearch`).

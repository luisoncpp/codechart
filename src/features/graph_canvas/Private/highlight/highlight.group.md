---
id: canvas_highlight
label: Source Highlighting
color: "#d97706"
icon: bolt
descriptionShort: Syntax tokens, diff rows & match highlights
---

Source-text rendering shared by L2 documents and preview frames: the lexical syntax highlighter with per-language rules, unified-diff code rows (`DiffCodeLines`), find-match span nesting inside syntax tokens, and the match stepper used by every find bar.

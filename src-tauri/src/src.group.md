---
id: backend_shell
label: Backend Shell
color: "#991b1b"
icon: box
facades:
  - lib.rs
descriptionShort: Crate root & Tauri entry
---

Library crate root (`lib.rs`) and desktop entrypoint (`main.rs`). Registers Tauri plugins and IPC handlers; re-exports the analysis deep modules for the CLI.

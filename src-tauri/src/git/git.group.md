---
id: git
label: Git
color: "#475569"
icon: git-branch
facades: []
descriptionShort: Git read seam for diff IPC
---

Read-only git helpers (log, diff, show) behind the Tauri IPC layer. Public helper module (`facades: []`) consumed by `tauri_api`.
